openai>=1.0.0

# --- Graph-JEPA refinement layer (src/graph_jepa) ---
# Heavy ML deps. The MockEncoder + synthetic-data path needs only numpy; torch
# and torch-geometric are required to train/score. FlagEmbedding is only needed
# for the real BGE-M3 encoder (--encoder bge).
numpy>=1.24
torch>=2.1
torch-geometric>=2.5
FlagEmbedding>=1.2  # BGE-M3 node encoder (optional; --encoder bge only)
