# Required for --provider openrouter (default)
OPENROUTER_API_KEY=sk-or-v1-...

# Required for --provider anthropic (and --batch mode)
ANTHROPIC_API_KEY=sk-ant-...

# Optional default when --provider is omitted
COOPERATIVE_KG_PROVIDER=openrouter
